# Lumen — no env vars required.
# The copilot uses a self-contained demo stream; nothing to configure.

# Optional: disable Next.js telemetry & version-check
# (helpful if you see "fetch failed" warnings from a corporate firewall)
NEXT_TELEMETRY_DISABLED=1
